---
spring:
    # SpringCloud Eureka Regist Name
    application:
        name: demo-microservice

eureka:
  client:
    serviceUrl:
      defaultZone: ${DISCOVERY_SERVER_URL:http://localhost:8761/eureka/}
  instance:
    preferIpAddress: true